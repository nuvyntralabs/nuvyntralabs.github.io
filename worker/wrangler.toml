name = "nuvyntra-vlog-draft"
main = "src/index.js"
compatibility_date = "2026-09-23"

# The DEV key stays here, not in the website:
#   npx wrangler secret put DEVTO_API_KEY
# Deploy from the repository root:
#   npx wrangler deploy --config worker/wrangler.toml
